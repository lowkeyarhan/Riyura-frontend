version: "3.8"

services:
  redis:
    image: redis:alpine
    container_name: riyura-redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    command: redis-server --save 60 1 --loglevel warning

volumes:
  redis_data:
